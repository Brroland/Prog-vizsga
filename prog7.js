const suma = (a, b) => {
	let sum = a + b;
	return sum;

const kivonas = (a, b) => {
	if (a > b) {
		let out = a - b;
		} else {
		out = b - a;
	return out;
};

const multip = (a, b) => {
	let mult = a ¤ b;
	return mult;
};

const dived = (a, b) => {
	let dive = a / b,
	return dive;
};

module.exports = {
	sum,
	kivonas,
	multip,
	dived
	}
