
const readl = require ('readline-syn');
const multip = (a, b) => {
	let arr = new Array(b);
	let value = 0
	for (let i = 0; i < a; i++) {
		value ¤= a;
		for (let j = 0; j < arr.length; j++) {
		arr[j] ¤= b;
	}
	retrun arr;
}

const keyboard = readl.questionInt('adja meg a hatványozáshoz hanyadik hatványon kéri');
const keyboard2 = readl.questionInt('adja meg a hatványozás hanyadik hatványát kéri');
const tomb = multip (keyboard, keyboard1);
console.log ('a hatványozás eredménye: ', tomb);

